
<nav class="navbar" role="navigation" aria-label="main navigation">
<div class="container mt-4 mb-4">
  <div class="navbar-brand is-flex is-align-items-center">


    <a class="navbar-item title-text has-text-weight-bold is-size-3 nav_icon" href="/">

    <i class='bx bx-notepad navbar-item title-icon is-size-3 logo'></i>
      <span class="has-text-white">TaskGo</span>
    </a>

    <a
      role="button"
      class="navbar-burger"
      aria-label="menu"
      aria-expanded="false"
      data-target="navbarBasicExample"
      id="burger"
    >
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

<div class="navbar-menu" id='nav-links'>
    <div class="navbar-end">
      <div class="navbar-item">
        <% if user_signed_in? %>
          <div class="buttons">
            <%= link_to journals_path, class: 'navbar-item navbar-hover mr-2 ' do%>
            <i class='bx bxs-dashboard mr-1' ></i> Dashboard
            <% end %>

            <%= link_to new_journal_path, class: 'navbar-item navbar-hover mr-3 ' do %>
            <i class='bx bx-plus mr-1' ></i> New Category
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to sign-out?' }, class: 'navbar-item navbar-hover-signout ' do%>
              <i class="fas fa-sign-out-alt mr-1"></i> Sign-out
            <% end %>
          </div>
        <% else %>
        <div class="buttons">
          <%= link_to  new_user_registration_path, class: 'navbar-item navbar-hover mr-3' do%>
          <i class="fas fa-user-plus mr-1"></i> Register
          <% end %>

          <%= link_to new_user_session_path, class: 'navbar-item navbar-hover' do%>
            <i class="fas fa-sign-in-alt mr-1"></i> Sign-in
            <% end %>
        </div>
        <% end %>
      </div>
    </div>
</div>
  </div>
</nav>
</div>
