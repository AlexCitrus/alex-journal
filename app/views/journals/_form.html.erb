<%= form_with(model: journal) do |form| %>
  <% if journal.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(journal.errors.count, "error") %> prohibited this journal
        from being saved:
      </h2>

      <ul>
        <% journal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field mb-3">
    <div class="control has-icons-left has-icons-right">
      
      <%= form.text_field :name, placeholder: "Journal Name", class: "input", autocomplete: :off %>
      <span class="icon is-left">
      <i class='bx bxs-pencil' ></i>
    </span>
    </div>
    </div>

    <div class="actions">
      <%= button_tag :submit, class:"button start-now-two" do %>
        <% if action_name == 'new'%>
          <i class="fas fa-plus mr-3"></i>
          Create Journal
        <% elsif action_name == 'edit' %>
          <i class="fas fa-plus mr-3"></i>
          Update Journal
        <% end %>
      <% end %>

      <%= link_to journals_path do%>
        <div class="button start-now-three">
          <i class="fas fa-undo mr-2"></i> Cancel
        </div>
      <% end %>
    </div>

<% end %>
